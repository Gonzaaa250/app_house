@model IEnumerable<app_house.Models.Localidad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Localidadname)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Localidadname)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Localidadid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Localidadid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Localidadid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
